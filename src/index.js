import React from 'react';
import ReactDOM from 'react-dom/client';
import './global.css';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <h1>Hello jusbrasil</h1>
  </React.StrictMode>
);
